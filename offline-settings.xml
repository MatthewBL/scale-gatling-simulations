<settings>
  <localRepository>${project.basedir}/local-repo</localRepository>
  <offline>true</offline>
  <mirrors>
    <mirror>
      <id>internal-mirror</id>
      <name>Internal Offline Mirror</name>
      <url>file://${project.basedir}/local-repo</url>
      <mirrorOf>*</mirrorOf>
    </mirror>
  </mirrors>
</settings>